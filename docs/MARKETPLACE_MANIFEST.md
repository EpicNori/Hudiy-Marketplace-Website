# Manifest

Use `schemaVersion`, a lowercase `id`, SemVer `version`, one supported package `type`, `entry.path`, `permissions`, and a `sha256:` checksum of the complete ZIP. Keep `manifest.json` at the ZIP root and use only relative package paths.
