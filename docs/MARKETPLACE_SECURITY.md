# Security

Service-role Supabase credentials are Edge Function secrets only. The public site uses the publishable key, validates basic input for user feedback, and relies on server-side authentication, ZIP validation, checksum verification, private Storage, and RLS.
