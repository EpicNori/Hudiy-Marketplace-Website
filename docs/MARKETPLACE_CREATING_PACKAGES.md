# Creating packages

Create a ZIP containing `manifest.json` and a declarative HTML/JavaScript entry under `app/`, `widget/`, or `overlay/`. Do not include native binaries or scripts. Calculate the checksum after the archive is final.
