# Publishing

An authenticated upload is stored privately and enters `pending_review`. Only moderation may promote both the plugin and its version to `published`; only then does the catalog return an install URL.
