# Installation

Installation is performed on the Hudiy device by the local Bridge. The website provides metadata and a short-lived signed URL; it never executes or copies package files on the device.
