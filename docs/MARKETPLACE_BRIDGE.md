# Bridge contract

The device Bridge exposes authenticated loopback endpoints for health, status, installed packages, install, update, uninstall, and reload. The website does not assume an undocumented `window.hudiy.installMarketplacePlugin` function.
